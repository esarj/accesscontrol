/**
 * This project uses the default Esarj ESLint configuration.
 *
 * You should not modify or override this configuration unless you have an absolutely critical and well-justified reason.
 * If you must customize the ESLint rules, you may import `@esarj/eslint-config/ts` and extend it in this file.
 * However, we strongly recommend sticking with the default configuration to ensure consistency across all Esarj projects.
 */
// eslint-disable-next-line import-x/no-default-export
export { default } from '@esarj/eslint-config/ts';
