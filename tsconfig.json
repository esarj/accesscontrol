{
  "extends": "@esarj/tsconfig",
  "compilerOptions": {
    "baseUrl": ".",
    "outDir": "./lib",
    "types": [
      "node",
      "jest"
    ]
  },
  "include": [
    // tsconfig.build.json only includes src/ dir.
    "src/**/*.ts",
    "test/**/*.ts"
  ]
}